import React from 'react';
import Header from '../Header/Header';
import Chefs from '../Chef/Chefs/Chefs';

const Home = () => {
    return (
        <div>
            <Header/>
            <Chefs/>
        </div>
    );
};

export default Home;